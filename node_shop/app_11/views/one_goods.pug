include init
title One goods
body
include header
main
    include nav
    div.container
        div.row
            div.col-6.cart-body
                h1.card-title #{good[0]['name']}
                img(
                    src='images/'+good[0]['image']
                    class="card-img-top"
                )
                p #{good[0]['cost']} UAN
                div.text-center
                    button.btn.btn-primary.add-to-cart(
                        data-goods_id=good[0]['id']
                    ) Go somewhere
            div.col-6.cart-body
                p #{good[0]['description']}
include footer